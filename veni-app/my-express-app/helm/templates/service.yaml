apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-minikube-app.fullname" . }}
  labels:
    {{- include "my-minikube-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-minikube-app.selectorLabels" . | nindent 4 }}
